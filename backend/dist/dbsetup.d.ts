/**
 * Creates the 'todos' table if it doesn't exist.
 */
export declare const createTasksTable: () => void;
/**
 * Creates the 'lists' table if it doesn't exist.
 */
export declare const createListsTable: () => void;
/**
 * Initializes the database by creating necessary tables.
 */
export declare const initializeDatabase: () => void;
